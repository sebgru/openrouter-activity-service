# openrouter-activity-service
# Add to your existing docker-compose.yml as an additional service file:
#   docker compose -f docker-compose.yml -f compose-addendum.yml up -d

services:
  openrouter-activity-service:
    image: ghcr.io/sebgru/openrouter-activity-service:latest
    restart: unless-stopped
    ports:
      - "8767:8767"
    volumes:
      # Create this file on the host:
      #   echo -n 'sk-or-v1-xxxxxxxxxxxx' > /home/sebg/openclaw/openrouter-management-token
      #   chmod 600 /home/sebg/openclaw/openrouter-management-token
      - /home/sebg/openclaw/openrouter-management-token:/run/secrets/openrouter-management-token:ro
    environment:
      OPENROUTER_ACTIVITY_PORT: "8767"
      OPENROUTER_MGMT_TOKEN_FILE: /run/secrets/openrouter-management-token
      OPENROUTER_WORKSPACE_ID: 73823bec-88a6-42e7-a146-0b1aa1ae0de0
    networks:
      - ai-net
